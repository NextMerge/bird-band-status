name: run-tests

on:
  push:
    branches:
      - main

jobs:
  my-job:
    name: install and run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4

      - run: pnpm install
      - run: pnpm test
